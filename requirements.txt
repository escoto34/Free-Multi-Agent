# Multi-Agent Ecosystem — Dependencies
# Python 3.11+ required

# --- Orchestration ---
langgraph>=0.4.0
langgraph-checkpoint-sqlite>=2.0.0

# --- LLM Provider SDKs ---
openai>=1.30.0            # Used for Groq and OpenRouter (OpenAI-compatible API)
cohere>=5.0.0             # Cohere Client V2 — NO v1 connectors

# --- Structured Data ---
pydantic>=2.7.0

# --- Git Operations (System A rollback) ---
gitpython>=3.1.0

# --- Configuration & Environment ---
python-dotenv>=1.0.0
mcp>=1.0.0
pyyaml>=6.0.0

# --- CLI / Interactive TUI ---
click>=8.1.0
textual>=0.80.0
rich>=13.0.0

# --- HTTP / Scraping ---
requests>=2.31.0               # Core HTTP client (core/requests_client.py)
scrapy>=2.11.0                 # Static crawling (scrapers/scrapy_project/)
selenium>=4.20.0               # Headless JS rendering (tools/selenium_driver.py)
webdriver-manager>=4.0.0       # Auto-download chromedriver

# --- Background Tasks ---
celery>=5.4.0                  # Distributed task queue
redis>=5.0.0                   # Redis broker + backend

# --- Deep Research Engine ---
gpt-researcher>=1.0.0          # Alternative research engine (agents/deep_research/)

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
respx>=0.22.0             # Mock HTTP for OpenAI-compatible clients (Groq, OpenRouter)
