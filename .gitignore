# Environment & secrets (live keys — only .env.example is tracked)
.env
.env.*
!.env.example
!.env.example.*
*.pem
*.key
secrets/
credentials/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
pip-wheel-metadata/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/
ENV/
.python-version

# Packaging / build
dist/
build/
*.whl

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace
*.code-workspace

# SQLite databases (quota tracking, checkpoints, run history)
*.db
*.sqlite
*.sqlite3
# Local runtime data: quotas.db, checkpoints.db, runs.db, logs/, vibe_last_failed/
data/

# OS
.DS_Store
.DS_Store?
Thumbs.db
desktop.ini

# Test & coverage
.pytest_cache/
.cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
.tox/
.nox/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytype/
.pyre/
.dmypy.json
dmypy.json

# Logs & local junk
*.log
*.bak
*.orig
*~
*.tmp
*.temp

# Generated knowledge graph (local graphify tooling; not product source)
# Includes graph.json, graph.html, GRAPH_REPORT.md, cache/, cost.json, manifests
graphify-out/
# If graphify-out/ was ever force-added, prefer: git rm -r --cached graphify-out

# Graphify / agent sidecars if ever written outside graphify-out/
.graphify_python
.graphify_root
.graphify_detect.json
.graphify_extract.json
.graphify_ast.json
.graphify_semantic.json
.graphify_analysis.json
.graphify_labels.json
.graphify_chunk_*.json
.needs_update

# Ad-hoc local scratch (not part of the product)
scratch/
tmp/
temp/
.scratch/
# Editor / agent local notes (keep SESSION_CHANGES.md local if used)
SESSION_CHANGES.md

# Local overrides (optional user copies — never commit live secrets)
config/model_router.local.yaml
config/model_benchmarks.local.yaml
config/cli_toolbox.local.yaml
config/*.local.yaml

# Docker (optional local override)
.env.docker
docker-compose.override.yml

# Editor / agent session noise under the repo (if tools dump here)
.claude/settings.local.json
.cursor/
.codex/
.grok/sessions/
*.session.md

# Jupyter / notebooks checkpoints
.ipynb_checkpoints/

# OS / package manager lock noise (keep intentional lockfiles if added later)
# poetry.lock and uv.lock may be committed when packaging is formalized
# Generated demo sites / local experiments (not product source)
credental-clinic/
credental_site/
credental-site/

